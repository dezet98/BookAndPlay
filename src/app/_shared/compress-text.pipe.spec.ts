import { CompressTextPipe } from './compress-text.pipe';

describe('CompressTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CompressTextPipe();
    expect(pipe).toBeTruthy();
  });
});
